server:
  port: 8083

spring:
  application:
    name: provider-service

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    root: info
    org.springframework.security: info
