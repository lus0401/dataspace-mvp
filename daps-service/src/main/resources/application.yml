server:
  port: 8081

spring:
  application:
    name: daps-service

management:
  endpoints:
    web:
      exposure:
        include: health,info

logging:
  level:
    root: info
    org.springframework.security: info
